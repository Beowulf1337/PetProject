import { registerRootComponent } from 'expo';
import App from './App';

// register the App component as the root component which will be rendered first when the app starts
registerRootComponent(App);
